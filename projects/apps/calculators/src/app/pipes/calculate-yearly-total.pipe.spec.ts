import { CalculateYearlyTotalPipe } from './calculate-yearly-total.pipe';

describe('CalculateYearlyTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateYearlyTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
